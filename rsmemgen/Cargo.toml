[package]
name = "rsmemgen"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true


[dependencies]
syn = { version = "^1", features = ["full", "parsing"] }
lazy_static = "^1.4.0"
proc-macro2 = { version = "1.0.32", default-features = false }
rand = "^0.8.5"
bytes = "^0.5.3"
chrono = "^0.4.0"
regex = "^1"


[features]
debug_mode = []